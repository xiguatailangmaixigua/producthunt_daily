{
  "terminal.integrated.env.osx": {
    "PATH": "${env:HOME}/.n/bin:${env:PATH}"
  },
  "terminal.integrated.env.linux": {
    "PATH": "${env:HOME}/.n/bin:${env:PATH}"
  },
  "terminal.integrated.env.windows": {
    "PATH": "${env:HOME}/.n/bin;${env:PATH}"
  },
  "terminal.integrated.defaultProfile.osx": "zsh",
  "terminal.integrated.profiles.osx": {
    "zsh": {
      "path": "/bin/zsh",
      "args": ["-l"]
    }
  }
}
